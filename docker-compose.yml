services:
  coredns:
    image: coredns/coredns:latest
    command: -conf /etc/coredns/Corefile
    ports:
      - "53:53/udp"
      - "53:53/tcp"
    volumes:
      - ./coredns/Corefile:/etc/coredns/Corefile:ro
      - ./coredns/zones:/etc/coredns/zones
    environment:
      - AGENTDNS_DOMAIN=${AGENTDNS_DOMAIN:-routeroot.dev}
    restart: always

  caddy:
    image: caddy:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - caddy_data:/data
      - caddy_config:/config
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
    environment:
      - AGENTDNS_DOMAIN=${AGENTDNS_DOMAIN:-routeroot.dev}
    restart: always

  agent-api:
    build: ./agent-api
    ports:
      - "8053:8053"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./coredns/zones:/dns-zones
      - ./data:/data
    environment:
      - AGENTDNS_DOMAIN=${AGENTDNS_DOMAIN:-routeroot.dev}
      - AGENTDNS_SERVER_IP=${AGENTDNS_SERVER_IP}
      - AGENTDNS_API_KEY=${AGENTDNS_API_KEY}
      - AGENTDNS_MAX_DEPLOYMENTS=${AGENTDNS_MAX_DEPLOYMENTS:-20}
      - AGENTDNS_DEFAULT_TTL=${AGENTDNS_DEFAULT_TTL:-48h}
      - AGENTDNS_MAX_MEMORY=${AGENTDNS_MAX_MEMORY:-2048}
      - AGENTDNS_MAX_CPUS=${AGENTDNS_MAX_CPUS:-2}
      - AGENTDNS_CADDY_ADMIN=http://caddy:2019
      - AGENTDNS_GITHUB_WEBHOOK_SECRET=${AGENTDNS_GITHUB_WEBHOOK_SECRET:-}
      - AGENTDNS_LOG_FORMAT=${AGENTDNS_LOG_FORMAT:-human}
      - DATABASE_PATH=/data/agentdns.db
      - ZONE_FILE_PATH=/dns-zones/db.${AGENTDNS_DOMAIN:-routeroot.dev}
    depends_on:
      - coredns
      - caddy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8053/api/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    restart: always

volumes:
  caddy_data:
  caddy_config:
